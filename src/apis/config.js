const config = {
    baseURL: 'http://localhost:8080/api',
    endpoints: {
      roadmaps: '/roadmaps',
      register: '/register',
      login: '/login_check',
      users: '/users',
      events: '/events'
    }
  };
  
  export default config;